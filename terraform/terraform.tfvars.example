project_id = "your-gcp-project-id"
region     = "us-central1"
zone       = "us-central1-a"

common_labels = {
  environment = "prod"
  service     = "crypto-compliance"
}

# Stack defaults like enabled APIs, resource names, and sizing now live in
# terraform/main.tf.
postgres_password = "change-me"
